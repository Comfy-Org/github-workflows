name: Bump assign-reviewers callers

# When the assign-reviewers reusable workflow is updated on main, open a
# SHA-bump PR in every repo that pins a caller against it. PRs are opened by
# Cloud Code Bot so they are easy to filter and merge.
#
# This is a thin entrypoint over the shared bumper at
# .github/bump-callers/bump-callers.sh — the same script drives the
# cursor-review, agents-md-integrity, AND pr-size caller fleets
# (bump-cursor-review-callers.yml, bump-agents-md-callers.yml,
# bump-pr-size-callers.yml). Keeping ONE implementation is deliberate: a forked
# copy is how other shared machinery in the org has drifted. The entrypoints
# differ only in their path-filter trigger and the parameters passed below.
# (They stay separate rather than a single matrix because their triggers differ
# — an assign-reviewers.yml change must not spuriously bump the other fleets'
# callers, and vice versa.)
#
# The caller list is NOT hardcoded here. This repo is PUBLIC (workflow file and
# Actions run logs are both publicly viewable), and most callers are private, so
# their names must never appear in this file or its logs. The list lives in the
# repo-level Actions variable `ASSIGN_REVIEWERS_CALLERS` (config, not a
# credential — a variable, not a secret, since secrets are write-only via the
# API) as a JSON array of {"repo","file","label"} objects, same shape as
# CURSOR_REVIEW_CALLERS. Every repo name is `::add-mask::`ed out of the (public)
# run logs before it is ever echoed. The variable is seeded EMPTY (`[]`) — an
# empty list is a clean no-op here (ALLOW_EMPTY below); the rollout's per-repo
# caller tickets add entries as callers land (today only Comfy-Org/cloud pins a
# caller).
#
# Update flow — adding/removing a caller needs NO public commit:
#   gh variable set ASSIGN_REVIEWERS_CALLERS --repo Comfy-Org/github-workflows \
#     --body "$(jq -c . callers.json)"
# Keep the canonical callers.json in a PRIVATE infra/ops repo so variable edits
# have a reviewed source of truth; the org audit log records each edit. (The
# specific home repo is intentionally not named here — this file is public.)

on:
  workflow_dispatch: {}   # allow on-demand runs (e.g. to re-bump callers)
  push:
    branches: [main]
    paths:
      # assign-reviewers.yml is entirely self-contained — its selection logic
      # runs inline via actions/github-script, with no external prompt/script
      # asset dir (unlike cursor-review/** or scripts/check-pr-size/**). So the
      # workflow file itself is the only path that can change its behavior.
      - .github/workflows/assign-reviewers.yml

permissions:
  contents: read

# Serialize runs of this fleet. The bumper now pushes to a STABLE branch
# (ci/bump-assign-reviewers) shared across runs, so two overlapping runs (a
# rapid second main push, or a push racing a manual re-run) would force-reset
# that branch and race the PR update — an older run finishing last could leave
# the committed diff pinned to a stale SHA. cancel-in-progress: false lets the
# running bump finish; GitHub keeps only the newest pending run, so the latest
# SHA always wins (BE-3882).
concurrency:
  group: bump-assign-reviewers-callers
  cancel-in-progress: false

jobs:
  bump:
    # Guard against a workflow_dispatch run from a non-main ref: github.sha is
    # the tip of whatever ref was selected, and the bumper force-resets a stable
    # shared branch — a manual run from an older ref would pin every caller to a
    # stale SHA (and run this job's script, with the App token minted below,
    # from an unreviewed commit). The push trigger is already main-only, so this
    # only ever skips stray manual runs.
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Generate Cloud Code Bot token
        uses: actions/create-github-app-token@d72941d797fd3113feb6b93fd0dec494b13a2547 # v1.12.0 — pinned: see dependabot.yml ignore (v2+ breaks cross-repo `owner:` token scoping)
        id: token
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.CLOUD_CODE_BOT_PRIVATE_KEY }}
          owner: Comfy-Org

      # A SECOND app token, for the ROSTER READ ALONE. Scoped to THIS repo
      # (`repositories: github-workflows` — public, so nothing leaks by naming it)
      # and deliberately left un-downscoped, so it carries the App's
      # "Variables: read" grant: the `app-permissions` schema — the
      # installation-token request body — has no `variables` key at all, so there
      # is no `permission-variables` input to ask for in ANY version of
      # create-github-app-token, and asking for nothing is the only spelling that
      # yields that grant (BE-6477).
      #
      # It exists so the WRITE token above can stay downscoped. One token serving
      # both purposes would have to be un-downscoped AND owner-wide — full
      # installation permissions on EVERY repo the App is installed on, most of
      # them private callers — purely to enable one read of this repo's own
      # variable. This token is un-downscoped too, but `repositories:` confines it
      # to this ONE public repo, so that is the whole of its blast radius.
      - name: Generate roster-read token (this repo only)
        uses: actions/create-github-app-token@d72941d797fd3113feb6b93fd0dec494b13a2547 # v1.12.0 — pinned: see dependabot.yml ignore (v2+ breaks cross-repo `owner:` token scoping)
        id: var_token
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.CLOUD_CODE_BOT_PRIVATE_KEY }}
          owner: Comfy-Org
          repositories: github-workflows

      - name: Bump SHA in caller repos
        env:
          GH_TOKEN: ${{ steps.token.outputs.token }}
          # The this-repo-only token, used ONLY for the roster read, so the write
          # token above can stay downscoped (BE-6482 review).
          VAR_TOKEN: ${{ steps.var_token.outputs.token }}
          NEW_SHA: ${{ github.sha }}
          # Names the fleet's Actions variable. bump-callers.sh READS the roster
          # out of it at run time (BE-6482) — it is deliberately NOT passed in
          # through this `env:` block, because Actions prints a step's env into
          # this PUBLIC repo's run log BEFORE the step runs, i.e. before the
          # script's `::add-mask::` could cover the private caller names.
          VAR_NAME: ASSIGN_REVIEWERS_CALLERS
          TAG: assign-reviewers
          WORKFLOW_FILE: assign-reviewers.yml
          # Seeded empty; an empty list is a clean no-op, not an error.
          ALLOW_EMPTY: "true"
        run: |
          # The main-only ref guard above cannot catch a manual RE-RUN of an
          # older main run: github.ref is refs/heads/main but github.sha is
          # that run's original (now stale) commit, and the bumper would
          # force-repin every caller to it. Only act from the current tip —
          # any newer push has its own queued run that does the bump.
          # Don't pipe into `cut`: the pipeline would report cut's status, so a
          # failed ls-remote (network blip, remote hiccup) yields an EMPTY
          # main_tip that then compares unequal to github.sha and silently
          # no-ops the whole fleet bump as if the run were stale. A lookup we
          # couldn't perform is not evidence of staleness — fail loudly.
          if ! ls_remote=$(git ls-remote origin refs/heads/main); then
            echo "::error::Could not look up the current main tip (git ls-remote failed)"
            exit 1
          fi
          main_tip=${ls_remote%%$'\t'*}
          if [[ -z "$main_tip" ]]; then
            echo "::error::git ls-remote returned no SHA for refs/heads/main"
            exit 1
          fi
          if [[ "$main_tip" != "$GITHUB_SHA" ]]; then
            echo "github.sha $GITHUB_SHA is not the current main tip ($main_tip) — stale run/re-run; nothing to bump"
            exit 0
          fi
          # The push path filter also matches a commit that DELETES the
          # reusable workflow; bumping callers to a SHA where the file is gone
          # would break every caller. Deletion means decommissioning — no-op.
          # (This fleet has no run-time asset dir — the yml is self-contained —
          # so unlike the sibling entrypoints only the workflow file is checked.)
          if [[ ! -f .github/workflows/assign-reviewers.yml ]]; then
            echo "assign-reviewers.yml absent at this SHA — decommissioned; nothing to bump"
            exit 0
          fi
          bash .github/bump-callers/bump-callers.sh
